# ✈️ Sistema de Gestión de Agencia de Viajes (POO en Java)

Este repositorio contiene la implementación incremental de un sistema de gestión para una Agencia de Viajes, desarrollado como parte del bootcamp de Programación Orientada a Objetos (POO) en Java.

---

## 🚀 Progresión del Proyecto

El proyecto ha avanzado semana a semana, aplicando los siguientes principios de POO:

| Semana | Concepto Principal Aplicado | Foco del Dominio |
| :--- | :--- | :--- |
| **01** | Clases, Objetos y Métodos | Definición de Entidades básicas (Cliente, Empleado). |
| **02** | Relaciones y Asociaciones | Conexión de Clases (Reserva, Servicio, Paquete). |
| **03** | Encapsulación | Uso de atributos `private`, *getters* y *setters*. |
| **04** | Herencia | Creación de jerarquías (`EmpleadoPlanta` y `EmpleadoContrato`). |
| **05** | Polimorfismo | Sobrecarga de métodos (ej. `buscarReserva`) y Sobrescritura (ej. `calcularSalario`). |
| **06** | **Abstracción e Interfaces** | **Clases Abstractas** (`ServicioTuristico`) e **Interfaces** (`Reservable`, `Calificable`). |

---

## 📦 Estructura de Carpetas

La estructura del proyecto está organizada por semanas, demostrando la evolución del código: